<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/febric_bear"
    tools:context=".MainActivity" >

    <EditText
        android:id="@+id/textCount"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="false"
        android:layout_marginTop="18dp"
        android:clickable="false"
        android:ems="1000"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:gravity="center_vertical|center_horizontal"
        android:inputType="numberSigned"
        android:text="0"
        android:textSize="@dimen/textSizeCounter" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/buttonDown"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/buttonUp"
        android:layout_alignBottom="@+id/buttonUp"
        android:layout_alignParentRight="true"
        android:text="▼" />

    <Button
        android:id="@+id/buttonUp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/textCount"
        android:layout_below="@+id/textCount"
        android:text="▲" />

    <EditText
        android:id="@+id/textDescription"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/buttonDown"
        android:layout_marginTop="51dp"
        android:ems="10"
        android:gravity="top|left"
        android:hint="Write anything"
        android:inputType="textMultiLine"
        android:scrollHorizontally="false"
        android:selectAllOnFocus="false" />

    <Button
        android:id="@+id/buttonResetMemo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:text="Reset Memo" />

    <Button
        android:id="@+id/buttonResetCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_toLeftOf="@id/buttonResetMemo"
        android:text="Reset Count" />

</RelativeLayout>