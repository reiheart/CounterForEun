<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layoutMain"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_witch_list"
    tools:context=".MainActivity" >

    <RelativeLayout
        android:id="@+id/layoutTitle"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white" >

        <ImageView
            android:id="@+id/imgTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:src="@drawable/bg_title"
            android:contentDescription="@string/cont_desc_image" />

        <TextView
            android:id="@+id/txtTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="30dp"
            android:text="@string/notice_start"
            android:textAppearance="?android:attr/textAppearanceLarge"
            android:textColor="@color/text_grey" />
    </RelativeLayout>

    <LinearLayout
        android:id="@+id/layoutSubMain"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical" >

        <LinearLayout
            android:id="@+id/layoutButtons"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp"
            android:gravity="right|center_vertical" >

            <Button
                android:id="@+id/buttonAdd"
                android:layout_width="80dp"
                android:layout_height="38dp"
                android:background="@drawable/bg_btn_up" />

            <Button
                android:id="@+id/buttonRemove"
                android:layout_width="80dp"
                android:layout_height="38dp"
                android:background="@drawable/bg_btn_down" />

        </LinearLayout>

        <ListView
            android:id="@+id/listCounting"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingLeft="10dp"
            android:paddingRight="10dp" >

        </ListView>

    </LinearLayout>
    
</RelativeLayout>